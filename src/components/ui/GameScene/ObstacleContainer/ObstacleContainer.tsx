import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const ObstacleContainer = () => {
  return (
    <View>
      <Text>ObstacleContainer</Text>
    </View>
  );
};

export default ObstacleContainer;

const styles = StyleSheet.create({});
